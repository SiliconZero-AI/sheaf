# 更新记录

> 英文版见 [CHANGELOG.md](CHANGELOG.md)。两份内容对应同一批改动，改一份就要改另一份——
> 发版时应用内的更新提示直接读这里的段落显示给中文用户看。

## 未发布

挂了哪些文件夹、读到哪儿，不再靠浏览器存储；正文放大的比例也记得住了。

- **手动升级不会再把工作区清掉。** 你开着哪些文件夹、上次写的是哪篇、每篇读到哪，以前存在浏览器引擎自己的存储里——而卸载页上那个「删除应用数据」复选框，一勾就把它整个删掉。现在这些搬进了那个目录之外的一份 `state.json`，升级时勾了也碰不到。
- **不用重新挂一遍。** 这一版第一次启动会把旧位置里的东西原样搬过来，之后以文件为准。你已经开着的文件夹照样开着。
- **正文放大的比例留得住。** 以前按 `Ctrl` 加 `+` 或 `-` 调大之后，关掉重开又回到 100%。现在你留在多大，下次就是多大。
- 万一这份文件坏了——手改坏、写一半断电——Sheaf 照样能起来，回头去旧存储里把能救的救回来，并重写出一份干净的。不会因为几个坏字节就开成一片空白。
- 有一件事它救不了以前：**升级到这一版**的时候如果勾了「删除应用数据」，旧数据还是会没——因为你正要离开的那一版从来没写过这个新文件。从这一版往后就一直在了。

## 0.1.6 — 2026-08-27

图太小看不清的，现在可以点开全屏看了。

- **图片和流程图鼠标移上去，右上角冒出一个按钮。** 点开就铺满整个窗口，滚轮缩放、按住拖着看、`Esc` 退回来。做这件事的起因是宽流程图——被压到正文栏那么宽之后，字全挤成一团，正文再怎么放大它也纹丝不动。
- **打开时按「装得下」的比例摆好。** 小图保持原尺寸，不会被吹大成一团糊；大图缩到整张看得见为止。`0` 回到这个比例，`+` `-` 一档一档调，当前百分比一直显示着。
- 滚轮缩放以鼠标为准——指着流程图的哪个角，放大之后那个角还在鼠标底下。
- 稿子怎么存、怎么导出、怎么打印，一个字没动：这是多了一种看图的方式，不是改了图在页面上的样子。

## 0.1.5 — 2026-08-26

点了格式当场就能看到效果，正文也能放大了。

- **加粗、斜体、删除线、链接现在当场渲染。** 以前选中一段字点工具栏（或者按 `Ctrl+B` / `Ctrl+I` / `Ctrl+D` / `Ctrl+K`），画布上会先冒出一串裸语法——`~~像这样~~`——非要去别处敲几个字才补渲染成效果。现在点完立刻就是最终的样子，单栏画布本来就该这样。
- **正文可以放大缩小了。** 按住 `Ctrl` 加 `+` 或 `-` 调大小，`Ctrl+0` 回到原始大小，按住 `Ctrl` 滚滚轮也行。稿子密、或者人离屏幕远的时候用得上。

## 0.1.4 — 2026-08-26

Sheaf 现在能自己升级自己了。

- **新版本会自己找上门。** 启动后不久 Sheaf 会悄悄查一次，有新版就问你要不要装。点一下就自动下载、安装、重新打开，回到你原来那篇稿子。不用再开浏览器去官网找安装包。
- **查更新不会打扰你。** 整个过程在后台安静进行——断网或者没查成，Sheaf 一声不吭。想主动查就去帮助面板，那里点一次一定给你结果。
- **动手装之前，先把你的稿子写回磁盘。** 安装要重启 Sheaf，所以它会先保存。万一存不下去——有一处外部改动你还没决定留哪份、文件已经从磁盘上消失、或者上次保存本来就失败了——更新会停下来并告诉你是哪一种，而不是带着没保存的内容重启。
- **提示框会告诉你这一版改了什么。** 按你的界面语言列出这次的更新说明，装之前就知道自己在装什么。
- 升级之后，你挂的文件夹和每篇读到哪都还在。

## 0.1.3 — 2026-08-25

写给一边开着 Sheaf、一边用 AI 工具改稿子的人。

- **在 Sheaf 外面改的内容，现在立刻就能看到。** 不用再切走再切回来 Sheaf 才发现——AI 工具或别的编辑器一存盘，正开着的这篇自己就更新了。一秒写好几次也会合并成一次更新，画面不会闪。
- **Sheaf 会停在你原来读的地方。** 重新载入时靠最近的标题定位，不是按像素，所以在你上面新增的文字不会把你顶走。
- **每篇都记得上次读到哪。** 换一篇、或者关掉重开，你会回到离开时的位置和光标处，而不是文件开头或最末尾。
- **外面新建的文件会自己出现在文件栏里。** 不用手动刷新。Sheaf 不会切过去——你正在写的那篇原样开着。
- **在外面被删掉的稿子，正文还留在屏幕上。** 自动保存暂停，状态栏告诉你这个文件在磁盘上已经没了。按 `Ctrl+S` 可以写回去，Sheaf 自己不会悄悄重建它。
- 帮助面板补上了这两条说明，不用离开 App 就能知道有什么变化。

## 0.1.2 — 2026-08-25

四件一直挡路的小事。

- **Sheaf 现在会显示自己的版本号。** 在帮助面板底部，不用去翻文件属性就知道自己装的是哪一版。
- **单独打开的那篇 `.md`，关掉重开还能回来。** 以前只记得文件夹里的稿子——单开一个散篇、关掉 Sheaf，再打开就跑到别处去了。现在回到的是你真正在写的那篇。
- **双击 `.md` 时窗口会站到最前，可以直接打字。** 以前 Sheaf 确实切过去了，但窗口还留在别的窗口后面，看着像什么都没发生。现在它会从任务栏还原、站到最前、并接管键盘，不用先点一下。
- **打开一个位于已挂文件夹里的文件，现在会认它属于那个文件夹。** 双击、拖进来、或者单开一个正好在已打开文件夹里的文件，Sheaf 会在文件栏里高亮它并正确解析它的图片。以前会被当成无归属的散篇，结果写成相对路径的图全都加载不出来。

## 0.1.1 — 2026-08-25

修掉一条会丢稿的路。如果你会在 Sheaf 之外改自己的 `.md`，建议更新。

- **自动保存不会再覆盖外部改动。** 写盘前先看一眼磁盘上那份。趁你不在时它被改过的话，自动保存会停下来问你留哪一份，而不是默默盖掉。
- **切回来时文件会重新载入。** 切到别的程序、在那边改了、再切回来——Sheaf 会读到新内容并保持你的滚动位置。你手上也有未保存改动的话，它会先问。
- 冲突提示把两份内容并排摆出来，各自的长度、改动时间和开头第一行都写着，选哪份不用靠猜。

## 0.1.0 — 2026-08-20

首个公开版本，仅 Windows。

- 打开一个或多个文件夹，在一块即时渲染的画布上写
- 自动保存；图片落到稿件旁边的 `images/`，正文里写相对路径
- 大纲、当前稿内查找、跨已打开文件夹搜索
- 导出 `.md`、`.html`，或直接打印成 PDF
- 深浅两套主题，中英文界面
